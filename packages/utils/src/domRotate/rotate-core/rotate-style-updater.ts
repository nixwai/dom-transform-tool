import type { DomRotateStyle } from '../types';

export class RotateStyleUpdater {
  // private targetRef?: WeakRef<HTMLDivElement>;

  public setStyleRotate: (value: number) => DomRotateStyle = () => ({});
}
